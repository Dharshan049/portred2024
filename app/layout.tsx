import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Dharshan R E - AI Developer, Machine Learning Enthusiast, and Computer Vision Specialist",
  description:
    "Explore the portfolio of Dharshan R E, an AI Developer, Machine Learning Enthusiast, and Computer Vision Specialist. Discover innovative projects, advanced AI solutions, and cutting-edge machine learning applications in the field of computer science.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
